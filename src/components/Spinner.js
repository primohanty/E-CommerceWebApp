import "./Spinner.css"

const Spinner = () => {
    return (
        <div className="h-[100vh] flex justify-center items-center">
            <div className="spinner">
            </div>

        </div>
    )
}

export default Spinner
